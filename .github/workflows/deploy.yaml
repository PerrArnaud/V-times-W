name: Deploy to Portainer

on:
  push:
    branches:
      - main  # Changez selon votre branche principale (main/master)

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Portainer Webhook
        run: |
          curl -X POST ${{ secrets.PORTAINER_WEBHOOK_URL }}
